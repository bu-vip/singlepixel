'use strict';

describe('myApp.rawDataView module', function() {

  beforeEach(module('myApp.rawDataView'));

  describe('rawDataView controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var rawDataViewCtrl = $controller('rawDataViewCtrl');
      expect(rawDataViewCtrl).toBeDefined();
    }));

  });
});