'use strict';

describe('myApp.configView module', function() {

  beforeEach(module('myApp.configView'));

  describe('configView controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var configViewCtrl = $controller('configViewCtrl');
      expect(configViewCtrl).toBeDefined();
    }));

  });
});